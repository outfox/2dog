<Project>
    <!-- Transitive consumers of 2dog.engine get the full targets too, so GodotPlugins is copied for every host. -->
    <Import Project="$(MSBuildThisFileDirectory)../build/2dog.engine.targets"/>
</Project>
