{
  "$comment": "Any .NET 10 SDK works: latestFeature rolls forward to the newest installed 10.0.* band (that band needs the wasm-tools workload to publish the web host)",
  "sdk": {
    "version": "10.0.100",
    "rollForward": "latestFeature"
  }
}
