{
  "$comment": "Pins the SDK band that has the wasm-tools workload installed (and thereby the Emscripten version matching <EmscriptenVersion> in Directory.Build.props). Run dotnet from within this directory so this file wins over the repo root global.json.",
  "sdk": {
    "version": "10.0.100",
    "rollForward": "latestFeature"
  }
}
