{
  "$comment": "Same pin as the repo root global.json (nearest wins, so this keeps a publish started inside this directory working even if the root pin changes): the SDK band whose wasm-tools workload matches <EmscriptenVersion> in Directory.Build.props.",
  "sdk": {
    "version": "10.0.100",
    "rollForward": "latestFeature"
  }
}
